apiVersion: tensor-fusion.ai/v1
kind: TensorFusionCluster
metadata:
  labels:
    app.kubernetes.io/name: tensor-fusion
    app.kubernetes.io/managed-by: kustomize
  name: tensorfusioncluster-sample
spec:
  # TODO(user): Add fields here
